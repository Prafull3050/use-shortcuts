# ⚡ use-shortcut — React Custom Keyboard Shortcut Manager

A lightweight and flexible **custom keyboard shortcut handler** for React applications.  
Easily register and deregister shortcuts anywhere in your component tree.

---

## 🚀 Features

- Register multiple shortcuts from any component
- Deregister shortcuts dynamically
- Global event listener with optimized matching
- Prevents duplicate shortcut registration
- Fully typed (TypeScript support)
- Clean and simple API: `const { registerKey, deregisterKey } = useShortcut()`

---

## 📦 Installation

```bash
npm install use-shortcut
# or
yarn add use-shortcut
```
